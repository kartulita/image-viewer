(function () {
	'use strict';

	/*
	 * Not an angular module, this file exists merely to keep my build-system
	 * happy (as this is part of a mostly angular project, each module is
	 * expected to contain a `module.js` file which defines the angular module
	 * and its dependencies.
	 */

})();
